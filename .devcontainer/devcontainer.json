{
  "name": "Architetture25",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "features": {
    "ghcr.io/devcontainers/features/python:1": { "version": "3.11" }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y build-essential nasm python3-dev python3-venv && python -m venv ProgettoGruppoX/venv && . ProgettoGruppoX/venv/bin/activate && pip install -U pip numpy setuptools wheel",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-python.python"
      ]
    }
  }
}
